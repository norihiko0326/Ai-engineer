# 機能要件・非機能要件

## 機能要件

### 1. タスク表示機能
- 3つの列を横並びで表示
  - 「やることリスト」列
  - 「進行中」列
  - 「完了」列
- 各列の下にタスクを表示（カード形式）

### 2. タスク追加機能
- テキスト入力欄とボタンを用意
- ボタンをクリック、またはEnterキーでタスクを「やることリスト」に追加
- 追加後、入力欄をクリアする

### 3. タスク削除機能
- 各タスクカードに削除ボタンを表示
- 削除ボタンをクリックするとそのタスクを削除

### 4. タスク移動機能（ドラッグ＆ドロップ）
- マウスでタスクをドラッグして、異なる列にドロップで移動

### 5. データ保存機能
- タスク情報をブラウザのローカルストレージに保存
- ページをリロードしてもタスクが残る

---

## 非機能要件

### UI・UX
- レスポンシブデザイン（スマートフォンでも表示可能）
- 見やすい色使い（列ごとに異なる背景色）
- ホバーエフェクト（ボタンやタスクにマウスを乗せた時の変化）

### パフォーマンス
- タスク操作時に瞬時に画面が更新される
- 100個程度のタスクでも動作する
