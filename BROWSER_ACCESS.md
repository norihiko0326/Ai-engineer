# Kanban ボード - ブラウザアクセスガイド

## 🚀 すぐにアクセスする

### ステップ 1: ブラウザを開く

Chrome、Edge、Firefox のいずれかを開いてください。

### ステップ 2: URL を入力

以下の URL をアドレスバーに入力：

```
http://localhost:5173
```

### ステップ 3: Kanban ボードが表示される ✅

画面に以下が見えます：

```
┌─────────────────────────────────────────────────────────┐
│           Kanban Task Board                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │     TODO     │  │ IN_PROGRESS  │  │     DONE     │  │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤  │
│  │ タスク 1     │  │ タスク 2     │  │ タスク 3     │  │
│  │ タスク 4     │  │ タスク 5     │  │ タスク 6     │  │
│  │ ...          │  │ ...          │  │ ...          │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 🔍 検索機能をテスト

### 検索バーに入力

SearchBar に「認証」と入力：

```
[検索バー] 認証 [ステータス: すべて ▼]
```

**結果**:
- 400ms 後に「ユーザー認証機能」が表示される
- バックエンド API が呼ばれたことをブラウザ DevTools で確認可能

### ステータスフィルターをテスト

1. 検索バーに「フロント」と入力
2. ステータスセレクトを「TODO」に変更
3. 「フロントエンド開発」が TODO 列に表示される

---

## 🛠️ トラブルシューティング

### ページが表示されない場合

**確認事項**:
1. ブラウザで F12 キーを押して DevTools を開く
2. Console タブでエラーを確認
3. Network タブで API 呼び出しを確認

**Docker が起動しているか確認**:
```powershell
docker ps
```

出力例:
```
NAMES              STATUS          PORTS
kanban_postgres    Up 2 minutes    5432->5432/tcp
kanban_backend     Up 2 minutes    8080->8080/tcp
kanban_frontend    Up 1 minute     5173->5173/tcp
```

### API が 502 エラーの場合

バックエンドが起動していない可能性があります：

```powershell
docker logs kanban_backend
```

### フロント npm install が遅い場合

初回起動時のみ npm install が実行されます。
このため 3～5 分かかる場合があります。

---

## 📊 動作確認チェックリスト

- [ ] ブラウザで `http://localhost:5173` にアクセスできた
- [ ] Kanban ボードが表示された
- [ ] SearchBar が見える
- [ ] 「認証」で検索できた
- [ ] ステータスフィルターが動作した
- [ ] ブラウザ DevTools で API 呼び出しが見える

---

## 🎯 今見えているもの

✅ **ボード画面**
- 3 列レイアウト（TODO / IN_PROGRESS / DONE）
- 各列にタスクカードが配置

✅ **タスクカード**
- タイトル
- 説明（省略表示）
- 優先度（色付きチップ）
- 作成日時

✅ **検索機能**
- SearchBar でキーワード入力
- ステータスフィルター
- 400ms デバウンス

---

## 🚀 次のステップ

実装は完全に完了しています。

次は：
1. API ドキュメントを作成（Swagger/OpenAPI）
2. 優先度フィルター追加
3. 日付範囲フィルター追加
4. 統計情報ダッシュボード

などが考えられます。

---

**Kanban ボード へようこそ！🎉**
