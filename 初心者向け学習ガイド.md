# 🎓 AI Engineering Course - 初心者向け学習ガイド

このドキュメントは、**これまでやってきたことを、初心者でもわかるように** 説明しています。

> **このガイドの目的**  
> フルスタック Web アプリ開発の全体像を理解し、各ステップで何をしたかを把握すること

---

## 📚 目次

1. [プロジェクトって何？](#プロジェクトって何)
2. [これまでやったこと](#これまでやったこと)
   - [Step 1: 環境セットアップ](#step-1-環境セットアップ完了)
   - [Step 2: バックエンド API 実装](#step-2-バックエンド-api-実装完了)
   - [Step 3: GitHub ワークフロー設定](#step-3-github-ワークフロー設定完了)
   - [Step 4: フロントエンド実装](#step-4-フロントエンド実装完了)
3. [重要な言葉の説明](#重要な言葉の説明)
4. [次回やるべきこと](#次回やるべきこと)
5. [よくあるコマンド](#よくあるコマンド)

---

## 🏢 プロジェクトって何？

### シンプルに言うと：

**Kanban Task App（カンバンタスク管理アプリ）** を作っています。

```
📱 アプリの画面
┌─────────────────────────────────┐
│ 📌 Kanban タスク管理アプリ       │
├─────────────────┬───────────────┤
│ TODO (やること) │ IN_PROGRESS   │ DONE
│                 │ (やってる中)   │ (完了)
├─────────────────┼───────────────┤
│ □ 認証機能      │ □ フロント開発│ □ Docker設定
│ □ テスト作成    │ □ DB設計      │ □ ドキュメント
└─────────────────┴───────────────┘
```

**目的：** タスクを 3 つのグループに分けて管理すること

---

## ✅ これまでやったこと

### 🔧 Step 1: 環境セットアップ（完了）

**「アプリを作るための準備」をしました。**

#### ✅ やったことリスト

```
✅ Java と Spring Boot をインストール
   → アプリを作るためのツール

✅ PostgreSQL（データベース）をセットアップ
   → タスクの情報を保存する場所

✅ Docker で コンテナを起動
   → アプリとデータベースをパッケージ化して実行

✅ GitHub でバージョン管理を開始
   → コードの履歴を保存
```

**難しい言葉を簡単に：**
- **Java**: プログラミング言語（料理でいう「材料」）
- **Spring Boot**: アプリ作成のテンプレート（「料理のレシピ本」）
- **PostgreSQL**: データベース（「電子手帳」みたいなもの）
- **Docker**: アプリを箱に入れて配送すること（「お弁当箱」）
- **GitHub**: コードを保存するサーバー（「クラウドストレージ」）

---

### 🔌 Step 2: バックエンド API 実装（完了）

**「アプリの裏側（サーバー）」を作りました。**

#### ✅ 具体的に何を作ったか

**1️⃣ Task（タスク）データモデルを定義**

```
Task の情報：
├─ id         : タスク番号（1, 2, 3...）
├─ title      : タスクのタイトル（「ユーザー認証機能」など）
├─ status     : 状態（TODO / IN_PROGRESS / DONE）
├─ priority   : 優先度（1～5、数字が大きいほど優先度高）
├─ createdAt  : 作成日時
└─ updatedAt  : 更新日時
```

**2️⃣ API エンドポイント（8 個）を作成**

```
API とは：フロントエンドがサーバーに「データちょうだい」と言うための電話窓口
```

| 何をしたいか | API の電話番号 | 説明 |
|----------|----------|------|
| 全タスクを見たい | GET /api/tasks | 10個のタスク全部返す |
| 特定のタスク見たい | GET /api/tasks/1 | ID番号1のタスクだけ返す |
| やってる中のタスク見たい | GET /api/tasks/status/IN_PROGRESS | 状態が「やってる中」のタスク返す |
| 優先度が高いやつ見たい | GET /api/tasks/priority/1 | 優先度1のタスク返す |
| 新しいタスク追加したい | POST /api/tasks | 新しいタスク追加する |
| タスク更新したい | PUT /api/tasks/1 | ID1のタスク内容を変更 |
| タスク削除したい | DELETE /api/tasks/1 | ID1のタスク削除 |
| サーバー動いてるか確認 | GET /health | 「動いてます」と返す |

**3️⃣ テストデータ（10 個）を自動投入**

```sql
データベースに自動的にタスクを 10 個追加しました：
1. ユーザー認証機能 - IN_PROGRESS
2. データベース設計 - IN_PROGRESS
3. フロントエンド開発 - TODO
4. ドラッグ&ドロップ - TODO
5. テスト作成 - TODO
6. デプロイメント設定 - DONE
7. ドキュメント作成 - DONE
8. パフォーマンス最適化 - IN_PROGRESS
9. キャッシング実装 - TODO
10. セキュリティ対応 - TODO
```

**4️⃣ 実際に動くか確認（テスト完了）**

```bash
# 全タスクを取得テスト
curl http://localhost:8080/api/tasks
→ 10個のタスク JSON が返ってきた ✅

# 特定タスク取得テスト
curl http://localhost:8080/api/tasks/1
→ ID1のタスク情報が返ってきた ✅

# ステータスフィルタテスト
curl http://localhost:8080/api/tasks/status/IN_PROGRESS
→ IN_PROGRESS の 3 個タスクが返ってきた ✅

# ヘルスチェック
curl http://localhost:8080/health
→ {"status":"UP"} と返ってきた ✅
```

**わかりやすく言うと：**
- REST API = 「スマホアプリがサーバーにデータをもらうための仕組み」
- エンドポイント = 「窓口の番号」
- GET = 「データをちょうだい」
- POST = 「新しいデータ追加して」
- PUT = 「このデータ更新して」
- DELETE = 「このデータ削除して」

---

### 🔗 Step 3: GitHub ワークフロー設定（完了）

**「チームで安全にコード作業するルール」を決めました。**

#### ✅ ワークフロー

```
1️⃣ 新しい機能を作るときは Issue を作成
   例：「タスク読み取り API 実装」というチケット作成

2️⃣ 機能用のブランチを作成
   例：feature/add-task-read-api ブランチで作業

3️⃣ コードをコミット（保存）
   例：git commit -m "READ API 実装"

4️⃣ Pull Request（PR）を作成
   例：「この機能完成しました、確認してください」と申請

5️⃣ レビュー後、main ブランチにマージ
   例：OK したら本番コードに統合

6️⃣ ブランチ自動削除 + Issue 自動クローズ
   例：マージしたら不要なブランチ自動削除
```

**今回やったこと：**
```
✅ feature/add-task-read-api ブランチ作成
✅ 8個のファイル実装
✅ PR #3 作成
✅ main にマージ
✅ ブランチ削除＆Issue自動クローズ設定
```

---

## 💡 重要な言葉の説明

### プログラミング用語

| 言葉 | 意味 | 例 |
|------|------|-----|
| **コード** | プログラムの指示文 | Java, Python など |
| **コミット** | コードを保存する | `git commit -m "xxx"` |
| **プッシュ** | ローカルコードをサーバーに送信 | `git push origin main` |
| **プル** | サーバーのコードをダウンロード | `git pull origin main` |
| **ブランチ** | コードの作業用枝分かれ | feature/xxx, main など |
| **マージ** | 別ブランチのコードを統合 | 枝を幹に合わせる |
| **PR** | 「このコード本番に入れていい？」という申請 | Pull Request |

### アプリ開発用語

| 言葉 | 意味 | わかりやすく |
|------|------|---------|
| **バックエンド** | サーバー側の処理 | 「キッチン」（裏側） |
| **フロントエンド** | 画面・UI | 「お店の看板」（見た目） |
| **API** | サーバーとアプリの通信規格 | 「配達員が注文受け取る窓口」 |
| **エンドポイント** | API の通信先 | 「窓口の番号」 |
| **REST API** | よく使われる API のルール | 「標準的な配達方法」 |
| **データベース** | データを保存する場所 | 「デジタル棚」 |
| **Entity** | データベースのテーブル構造 | 「表のヘッダー」 |
| **Repository** | データベースアクセス層 | 「データ棚の管理人」 |
| **Controller** | API の処理を仕切る | 「レストランの店長」 |

### React・フロントエンド用語

| 言葉 | 意味 | わかりやすく |
|------|------|---------|
| **React** | フロントエンド JavaScript ライブラリ | 「画面作成工場」 |
| **Component** | 画面の部品（再利用可能） | 「組み立て部品」 |
| **JSX** | HTML 風の JavaScript 記述 | 「JavaScript + HTML の融合」 |
| **Props** | コンポーネント間のデータ受け渡し | 「部品への設定値」 |
| **State** | コンポーネント内の状態管理 | 「部品の記憶」 |
| **Hook** | 状態管理・副作用を扱う関数 | 「React の便利な機能」 |
| **Context** | 複数コンポーネント間での状態共有 | 「全体で使える共有メモ帳」 |
| **TypeScript** | JavaScript に型定義を追加 | 「より厳密な JavaScript」 |
| **Vite** | 高速開発サーバー | 「素早い開発環境」 |
| **Material-UI** | デザインシステム・UI ライブラリ | 「デザインテンプレート」 |

---

### 🎨 Step 4: フロントエンド実装（完了）

**「ユーザーが見て操作する画面」を作りました。**

#### ✅ 実装構成

```
現状：
✅ バックエンド（サーバー）完成
✅ フロントエンド（画面）完成
✅ API 連携完了

実装内容：
1. React + TypeScript で フロント実装 ✅
2. Kanban ボード UI（3列レイアウト）✅
3. リアルタイムデータ取得 ✅
4. 検索フィルター機能 ✅
5. タスク表示・管理機能 ✅
```

#### フロントエンド構成図

```
┌─────────────────────────────────────────┐
│ App.tsx （メインコンポーネント）         │
├─────────────────────────────────────────┤
│ ├─ SearchBar.tsx （検索フィルター）    │
│ └─ KanbanBoard.tsx （メインボード）    │
│    ├─ useTasks Hook                    │
│    │  ├─ TaskContext （状態管理）      │
│    │  └─ taskApi.ts （API通信）        │
│    │                                   │
│    ├─ KanbanLane.tsx × 3               │
│    │  ├─ TODO列                        │
│    │  ├─ IN_PROGRESS列                 │
│    │  └─ DONE列                        │
│    │                                   │
│    └─ TaskCard.tsx × 複数              │
│       └─ 個別タスク表示                │
└─────────────────────────────────────────┘
```

#### ファイル構成

```
frontend/src/
├── App.tsx                          ← メインコンポーネント
├── components/
│   ├── KanbanBoard.tsx             ← Kanban ボード本体（3列管理）
│   ├── KanbanLane.tsx              ← 個別レーン（TODO・IN_PROGRESS・DONE）
│   ├── TaskCard.tsx                ← タスクカード表示
│   └── SearchBar.tsx               ← 検索・フィルター
├── hooks/
│   └── useTasks.ts                 ← データ管理・API通信
├── context/
│   └── TaskContext.tsx             ← グローバル状態管理
├── api/
│   └── taskApi.ts                  ← バックエンドとの通信
└── types/
    └── task.ts                     ← TypeScript 型定義
```

### 技術スタック

```
フロントエンド：
✅ React 18 + TypeScript
✅ Vite （高速開発サーバー）
✅ Material-UI（MUI）（デザインシステム）
✅ Context API （状態管理）
✅ カスタム Hook （ロジック共有）
```

#### 実装済み機能

##### 1️⃣ Kanban ボード表示

```
画面構成：
┌─────────────────────────────────────────────┐
│ 📌 Kanban Task Board                        │
├─────────────────────────────────────────────┤
│ [検索バー]                                  │
├──────────────────┬──────────────────┬───────┤
│ TODO             │ 進行中           │ 完了  │
│ ┌──────────────┐ │ ┌──────────────┐ │ ┌────┐ │
│ │ ユーザー認証 │ │ │ フロント開発 │ │ │完了│ │
│ │ [P2]         │ │ │ [P1]         │ │ │    │ │
│ └──────────────┘ │ └──────────────┘ │ └────┘ │
│ [5 tasks]       │ [3 tasks]        │ [2]   │
└──────────────────┴──────────────────┴───────┘
```

**特徴：**
- 3 列のレイアウト（TODO・IN_PROGRESS・DONE）
- 各レーンにタスク数表示
- ステータスごとに色分け
- レスポンシブ対応（スマホは1列）

##### 2️⃣ タスクカード表示

```
1 つのタスク：
┌──────────────────┐
│ ユーザー認証機能 │
│ ID: 1            │
│ [優先度：2]      │
│ 作成: 2024-01-15 │
└──────────────────┘
```

**表示情報：**
- タスク ID
- タイトル
- 優先度
- ステータス
- 作成日時

##### 3️⃣ 検索・フィルター機能

```
SearchBar で以下が可能：
- テキスト検索（タイトル・説明）
- 優先度でフィルター
- ステータスでフィルター
```

#### 4️⃣ **API 連携**

```typescript
// バックエンド API との通信
GET /api/tasks          → 全タスク取得
GET /api/tasks/1        → 特定タスク取得
GET /api/tasks/status/? → ステータスフィルター
```

**データフロー：**
```
TaskContext（状態管理）
    ↑ ↓
useTasks Hook
    ↑ ↓
taskApi.ts（API通信）
    ↑ ↓
バックエンド（Spring Boot）
    ↑ ↓
PostgreSQL（データベース）
```

---

### 🌐 Step 5: 本番環境へのデプロイメント（完了）

**「作ったアプリを AWS クラウドで公開」しました。**

#### ✅ デプロイメントの流れ

```
Step 1: AWS 環境構築
└─ EC2 インスタンス起動（サーバー）
└─ RDS PostgreSQL 設定（データベース）

Step 2: バックエンド デプロイ
└─ Maven でビルド
└─ Docker イメージ作成
└─ EC2 コンテナで実行

Step 3: フロントエンド デプロイ
└─ Vite でビルド
└─ Nginx で静的ファイル配信

Step 4: 通信設定（重要！）
└─ Nginx リバースプロキシ設定
└─ API レスポンス形式を修正
└─ CORS・通信エラー対応
```

#### ⚠️ よくあるデプロイエラーと対処法

**1️⃣ ネットワークエラー（localhost:8080 に接続失敗）**

```
現象：
- ローカルでは動くが、本番環境（AWS）では白い画面
- ブラウザコンソール: "net::ERR_CONNECTION_REFUSED"

原因：
- フロントエンドが localhost:8080 にアクセスしようとしている
- 本番環境には localhost が存在しない

解決方法：
1. Nginx にリバースプロキシ設定を追加
   location /api/ → localhost:8080 へフォワード
2. フロントエンド環境変数を修正
   VITE_API_BASE_URL = "" （Nginx 経由で同じオリジン）
```

**2️⃣ API レスポンス形式の不一致**

```
現象：
- "e.filter is not a function" というエラー
- タスクが表示されない

原因：
- バックエンド: { success: true, data: [...] } で返す
- フロントエンド: response.data をそのままタスク配列と思っていた
- 実際には ApiResponse オブジェクトが返されたため失敗

解決方法：
1. taskApi.ts で実装を修正
   response.data.data にアクセスして実際のデータを抽出
2. 全 API メソッドで同じ修正を適用
```

#### デプロイ構成図

```
ユーザーのブラウザ
    ↓ (http://13.158.17.151)
Nginx（ポート 80）
    ├─ /             → フロントエンド静的ファイル配信
    └─ /api/*        → localhost:8080（バックエンド）へプロキシ
         ↓
Spring Boot（ポート 8080）
    ↓
RDS PostgreSQL
```

#### 確認事項（デプロイ後）

```
✅ ブラウザでアクセス可能
✅ Kanban UI が表示される
✅ タスク一覧が表示される
✅ API エラーなし（ブラウザコンソール確認）
✅ ネットワークリクエスト成功（200 ステータス）
```

---

## 🚀 次回アプリを作る時の流れ（テンプレート）

### フェーズ 1: 計画・設計（1-2 日）

```
1️⃣ 要件定義
   → アプリは何をするのか、ユーザーは誰か

2️⃣ 画面設計
   → UI・UX のスケッチ作成

3️⃣ データモデル設計
   → どんなデータを持つのか、テーブル構成

4️⃣ API 仕様書作成
   → エンドポイント一覧（GET/POST/PUT/DELETE）
```

### フェーズ 2: バックエンド実装（2-3 日）

```
1️⃣ プロジェクト初期化
   → Spring Boot プロジェクト作成

2️⃣ Entity（データベース構造）定義
   → @Entity, @Table, カラム定義

3️⃣ Repository（データ操作層）実装
   → JpaRepository を extends する

4️⃣ Service（ビジネスロジック層）実装
   → ビジネスルール・フィルタリング処理

5️⃣ Controller（API層）実装
   → @GetMapping, @PostMapping など

6️⃣ ローカルテスト
   → curl や Postman で API テスト
   → docker-compose up で動作確認
```

### フェーズ 3: フロントエンド実装（2-3 日）

```
1️⃣ React プロジェクト初期化
   → npm create vite@latest my-app

2️⃣ コンポーネント構成設計
   → どのコンポーネントが必要か整理

3️⃣ UI コンポーネント実装
   → Material-UI などを使用

4️⃣ API 連携（taskApi.ts など）
   → axios で HTTP リクエスト実装

5️⃣ 状態管理実装
   → Context API または Zustand

6️⃣ ローカルブラウザテスト
   → npm run dev で http://localhost:5173 確認
```

### フェーズ 4: 統合テスト（1 日）

```
1️⃣ ローカルで全機能テスト
   → バックエンド（docker-compose）+ フロントエンド（npm）

2️⃣ API 通信確認
   → ブラウザデベロッパーツール Network タブ確認

3️⃣ エラーハンドリング確認
   → 404, 500 エラー時の動作確認
```

### フェーズ 5: デプロイメント（1-2 日）

```
1️⃣ AWS インフラ構築
   → EC2（サーバー）、RDS（データベース）

2️⃣ バックエンドデプロイ
   → Maven ビルド → Docker イメージ → コンテナ実行

3️⃣ フロントエンドデプロイ
   → npm run build → Nginx 配信

4️⃣ 通信設定
   → Nginx リバースプロキシ設定
   → 環境変数修正（API ベース URL）
   → CORS 設定（必要に応じて）

5️⃣ 本番テスト
   → ブラウザで http://[AWS_IP] にアクセス
   → 全機能動作確認
```

### フェーズ 6: 運用・保守

```
1️⃣ ログ監視
   → EC2、RDS ログ確認

2️⃣ バグ報告・対応
   → Issue 作成 → ブランチ → PR → マージ

3️⃣ 定期バックアップ
   → RDS スナップショット取得
```

---

## 📋 各フェーズでよくある問題と対処法

### バックエンド開発時

| 問題 | 原因 | 対処法 |
|------|------|--------|
| **コンパイルエラー** | ビルドファイルの型エラー | `mvn clean compile` で再コンパイル |
| **DB 接続失敗** | postgres が起動していない | `docker-compose up -d postgres` |
| **API 404 エラー** | エンドポイント定義ミス | Controller の @RequestMapping 確認 |
| **CORS エラー** | フロント・バック別ドメイン | @CrossOrigin 追加 または Nginx CORS 設定 |

### フロントエンド開発時

| 問題 | 原因 | 対処法 |
|------|------|--------|
| **白い画面** | React レンダリング失敗 | ブラウザコンソールエラー確認 |
| **API 失敗** | localhost:8080 接続不可 | 環境変数（VITE_API_BASE_URL）確認 |
| **ビルドエラー** | TypeScript 型エラー | `npm run build` 実行してエラー箇所確認 |
| **コンポーネント更新されない** | State 更新忘れ | useEffect, useState の依存配列確認 |

### デプロイ時

| 問題 | 原因 | 対処法 |
|------|------|--------|
| **503 Bad Gateway** | Nginx → バックエンド接続失敗 | バックエンドコンテナ起動確認 |
| **ネットワークエラー** | localhost:8080 ハードコード | API ベース URL を環境に応じて変更 |
| **ポート競合** | 既に別プロセスが使用中 | `lsof -i :8080` で PID 確認して終了 |
| **CORS エラー** | フロント・バック別オリジン | Nginx リバースプロキシ設定で同一オリジンに |

---

## 🎓 次回アプリ開発時のチェックリスト

```
□ フェーズ 1: 計画（設計書作成）
  □ 要件定義書
  □ 画面ワイヤーフレーム
  □ DB スキーマ図
  □ API 仕様書

□ フェーズ 2: バックエンド
  □ Entity + Repository
  □ Service（ビジネスロジック）
  □ Controller（API）
  □ ローカルテスト OK

□ フェーズ 3: フロントエンド
  □ コンポーネント構成
  □ UI 実装
  □ API 連携（注意：response.data.data！）
  □ 状態管理
  □ ローカルブラウザテスト OK

□ フェーズ 4: 統合テスト
  □ 全エンドポイント動作確認
  □ ネットワークリクエスト確認
  □ エラーハンドリング確認

□ フェーズ 5: デプロイ
  □ AWS インフラ構築
  □ バックエンドデプロイ（Docker）
  □ フロントエンドデプロイ（Nginx）
  □ Nginx リバースプロキシ設定
  □ 環境変数修正（API ベース URL）
  □ 本番テスト OK（ブラウザで確認）
```

---

## さらに高度な機能の実装（オプション）

```
現状：
✅ バックエンド（サーバー）完成
✅ フロントエンド（画面）完成
✅ AWS デプロイ完了
✅ 基本的な表示・検索機能

次の拡張内容：
1. ドラッグ&ドロップ機能（タスク移動）
2. CREATE / UPDATE / DELETE 機能の UI
3. ユーザー認証（ログイン機能）
4. ダークモード対応
5. ページネーション（大量タスク対応）
```

**優先度の高い機能：**
```
高：ドラッグ&ドロップ（ユーザビリティ向上）
中：CRUD UI の実装（タスク操作）
中：ユーザー認証（セキュリティ）
低：ダークモード（快適性）
```

---

## 📝 よくあるコマンド

### ローカル作業（あなたのパソコン）

```bash
# ---- ブランチ作業 ----

# ブランチ一覧確認
git branch

# ブランチ作成 & 切り替え
git checkout -b feature/xxx

# main ブランチに戻る
git checkout main

# ---- コード保存 ----

# コードの状態確認
git status

# コード追加（ステージング）
git add .

# コミット（保存）
git commit -m "メッセージ"

# ---- サーバーとの連携 ----

# サーバーに送信（プッシュ）
git push origin feature/xxx

# サーバーから受信（プル）
git pull origin main

# ---- マージ ----

# PR マージ（GitHub CLI）
gh pr merge 3 --merge --delete-branch

# または main に戻ってマージ
git checkout main
git pull origin main
git merge feature/xxx
git push origin main
```

### Docker で起動（バックエンド）

```bash
# ディレクトリ移動
cd c:\AI\ Engineering\ Course\kanban-task-app

# コンテナ起動
docker-compose up --build

# コンテナ停止
docker-compose down

# ログ確認
docker logs kanban_backend
```

### npm で起動（フロントエンド開発サーバー）

```bash
# ディレクトリ移動
cd c:\AI\ Engineering\ Course\kanban-task-app\frontend

# 依存関係インストール（初回のみ）
npm install

# 開発サーバー起動
npm run dev

# ブラウザで確認
http://localhost:5173

# サーバー停止
Ctrl + C
```

### 本番ビルド（フロントエンド）

```bash
# ビルド実行
npm run build

# ビルド結果確認（dist フォルダに出力）
ls dist/

# プレビュー確認
npm run preview
```

### API テスト（PowerShell）

```powershell
# 全タスク取得
curl http://localhost:8080/api/tasks

# 特定タスク取得
curl http://localhost:8080/api/tasks/1

# ステータスフィルタ
curl http://localhost:8080/api/tasks/status/IN_PROGRESS

# 新しいタスク作成
$body = @{
    title = "新タスク"
    description = "説明"
    status = "TODO"
    priority = 1
} | ConvertTo-Json

curl -Method POST `
  -Uri "http://localhost:8080/api/tasks" `
  -ContentType "application/json" `
  -Body $body
```

---

## 🎯 理解度チェック

### 最新版 - 現在の実装状況

```
Q1: Kanban Task App は何を作るアプリ？
A: タスク（TODO・IN_PROGRESS・DONE）を管理するアプリ

Q2: バックエンドとは？
A: サーバー側の処理（API、データベース）

Q3: API の主な 8 つのエンドポイントは？
A: GET全件、GET指定ID、GET・POSTステータス、
   GET優先度、POST作成、PUT更新、DELETE削除、GET健康診断

Q4: Git のワークフローの順序は？
A: Issue作成 → ブランチ作成 → コミット → PR → マージ

Q5: フロントエンドは完成したか？
A: はい、React + TypeScript で Kanban ボード実装完了

Q6: フロントエンドの主なコンポーネントは？
A: App → KanbanBoard → KanbanLane × 3 → TaskCard × 複数

Q7: データはどのように管理されている？
A: TaskContext（状態管理）→ useTasks Hook → taskApi（API通信）

Q8: 次に作るべき機能は？
A: ドラッグ&ドロップ機能、CRUD UI、ユーザー認証など
```

すべて「A」が言えたら、さらに高度な機能実装の準備ができています！

---

## 📞 参考資料

1. **このファイル** ← 学習ガイド
2. **TECH_SPEC.md** ← 技術仕様（詳細版）
3. **CLAUDE.md** ← ワークフロー・開発ルール
4. **GitHub の Issues** ← 進行中の作業記録

---

## 🎓 最後に

今までやってきたことは、**フルスタック Web アプリ開発の基礎**です：

```
Step 1: 環境セットアップ  ✅
  └→ Java、Spring Boot、Docker など

Step 2: バックエンド実装  ✅
  └→ REST API、データベース設計

Step 3: Git ワークフロー  ✅
  └→ Issue → PR → マージの流れ

Step 4: フロントエンド実装  ✅
  └→ React、UI コンポーネント、API 連携
```

**次のステップ：**
- ドラッグ&ドロップ機能で UX を向上
- 認証機能でセキュリティ強化
- ユーザーデータの永続化

小さなステップを積み重ねるだけで、本格的なアプリになります。
習ったことを活かして、さらに高度な機能に挑戦してください！🚀
