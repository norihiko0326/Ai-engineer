# 🎓 AI Engineering Course - 初心者向け学習ガイド

このドキュメントは、**これまでやってきたことを、初心者でもわかるように** 説明しています。

---

## 📚 目次

1. [プロジェクトって何？](#プロジェクトって何)
2. [これまでやったこと](#これまでやったこと)
3. [重要な言葉の説明](#重要な言葉の説明)
4. [次回やるべきこと](#次回やるべきこと)
5. [よくあるコマンド](#よくあるコマンド)

---

## 🏢 プロジェクトって何？

### シンプルに言うと：

**Kanban Task App（カンバンタスク管理アプリ）** を作っています。

```
📱 アプリの画面
┌─────────────────────────────────┐
│ 📌 Kanban タスク管理アプリ       │
├─────────────────┬───────────────┤
│ TODO (やること) │ IN_PROGRESS   │ DONE
│                 │ (やってる中)   │ (完了)
├─────────────────┼───────────────┤
│ □ 認証機能      │ □ フロント開発│ □ Docker設定
│ □ テスト作成    │ □ DB設計      │ □ ドキュメント
└─────────────────┴───────────────┘
```

**目的：** タスクを 3 つのグループに分けて管理すること

---

## ✅ これまでやったこと

### 🔧 **Step 1: 環境セットアップ（完了）**

「アプリを作るための準備」をしました。

#### やったことリスト：

```
✅ Java と Spring Boot をインストール
   → アプリを作るためのツール

✅ PostgreSQL（データベース）をセットアップ
   → タスクの情報を保存する場所

✅ Docker で コンテナを起動
   → アプリとデータベースをパッケージ化して実行

✅ GitHub でバージョン管理を開始
   → コードの履歴を保存
```

**難しい言葉を簡単に：**
- **Java**: プログラミング言語（料理でいう「材料」）
- **Spring Boot**: アプリ作成のテンプレート（「料理のレシピ本」）
- **PostgreSQL**: データベース（「電子手帳」みたいなもの）
- **Docker**: アプリを箱に入れて配送すること（「お弁当箱」）
- **GitHub**: コードを保存するサーバー（「クラウドストレージ」）

---

### 🔌 **Step 2: バックエンド API 実装（完了）**

「アプリの裏側（サーバー）」を作りました。

#### 具体的に何を作ったか：

**1️⃣ Task（タスク）データモデルを定義**

```
Task の情報：
├─ id         : タスク番号（1, 2, 3...）
├─ title      : タスクのタイトル（「ユーザー認証機能」など）
├─ status     : 状態（TODO / IN_PROGRESS / DONE）
├─ priority   : 優先度（1～5、数字が大きいほど優先度高）
├─ createdAt  : 作成日時
└─ updatedAt  : 更新日時
```

**2️⃣ API エンドポイント（8 個）を作成**

```
API とは：フロントエンドがサーバーに「データちょうだい」と言うための電話窓口
```

| 何をしたいか | API の電話番号 | 説明 |
|----------|----------|------|
| 全タスクを見たい | GET /api/tasks | 10個のタスク全部返す |
| 特定のタスク見たい | GET /api/tasks/1 | ID番号1のタスクだけ返す |
| やってる中のタスク見たい | GET /api/tasks/status/IN_PROGRESS | 状態が「やってる中」のタスク返す |
| 優先度が高いやつ見たい | GET /api/tasks/priority/1 | 優先度1のタスク返す |
| 新しいタスク追加したい | POST /api/tasks | 新しいタスク追加する |
| タスク更新したい | PUT /api/tasks/1 | ID1のタスク内容を変更 |
| タスク削除したい | DELETE /api/tasks/1 | ID1のタスク削除 |
| サーバー動いてるか確認 | GET /health | 「動いてます」と返す |

**3️⃣ テストデータ（10 個）を自動投入**

```sql
データベースに自動的にタスクを 10 個追加しました：
1. ユーザー認証機能 - IN_PROGRESS
2. データベース設計 - IN_PROGRESS
3. フロントエンド開発 - TODO
4. ドラッグ&ドロップ - TODO
5. テスト作成 - TODO
6. デプロイメント設定 - DONE
7. ドキュメント作成 - DONE
8. パフォーマンス最適化 - IN_PROGRESS
9. キャッシング実装 - TODO
10. セキュリティ対応 - TODO
```

**4️⃣ 実際に動くか確認（テスト完了）**

```bash
# 全タスクを取得テスト
curl http://localhost:8080/api/tasks
→ 10個のタスク JSON が返ってきた ✅

# 特定タスク取得テスト
curl http://localhost:8080/api/tasks/1
→ ID1のタスク情報が返ってきた ✅

# ステータスフィルタテスト
curl http://localhost:8080/api/tasks/status/IN_PROGRESS
→ IN_PROGRESS の 3 個タスクが返ってきた ✅

# ヘルスチェック
curl http://localhost:8080/health
→ {"status":"UP"} と返ってきた ✅
```

**わかりやすく言うと：**
- REST API = 「スマホアプリがサーバーにデータをもらうための仕組み」
- エンドポイント = 「窓口の番号」
- GET = 「データをちょうだい」
- POST = 「新しいデータ追加して」
- PUT = 「このデータ更新して」
- DELETE = 「このデータ削除して」

---

### 🔗 **Step 3: GitHub ワークフロー設定（完了）**

「チームで安全にコード作業するルール」を決めました。

#### ルール：

```
1️⃣ 新しい機能を作るときは Issue を作成
   例：「タスク読み取り API 実装」というチケット作成

2️⃣ 機能用のブランチを作成
   例：feature/add-task-read-api ブランチで作業

3️⃣ コードをコミット（保存）
   例：git commit -m "READ API 実装"

4️⃣ Pull Request（PR）を作成
   例：「この機能完成しました、確認してください」と申請

5️⃣ レビュー後、main ブランチにマージ
   例：OK したら本番コードに統合

6️⃣ ブランチ自動削除 + Issue 自動クローズ
   例：マージしたら不要なブランチ自動削除
```

**今回やったこと：**
```
✅ feature/add-task-read-api ブランチ作成
✅ 8個のファイル実装
✅ PR #3 作成
✅ main にマージ
✅ ブランチ削除＆Issue自動クローズ設定
```

---

## 💡 重要な言葉の説明

### プログラミング用語

| 言葉 | 意味 | 例 |
|------|------|-----|
| **コード** | プログラムの指示文 | Java, Python など |
| **コミット** | コードを保存する | `git commit -m "xxx"` |
| **プッシュ** | ローカルコードをサーバーに送信 | `git push origin main` |
| **プル** | サーバーのコードをダウンロード | `git pull origin main` |
| **ブランチ** | コードの作業用枝分かれ | feature/xxx, main など |
| **マージ** | 別ブランチのコードを統合 | 枝を幹に合わせる |
| **PR** | 「このコード本番に入れていい？」という申請 | Pull Request |

### アプリ開発用語

| 言葉 | 意味 | わかりやすく |
|------|------|---------|
| **バックエンド** | サーバー側の処理 | 「キッチン」（裏側） |
| **フロントエンド** | 画面・UI | 「お店の看板」（見た目） |
| **API** | サーバーとアプリの通信規格 | 「配達員が注文受け取る窓口」 |
| **エンドポイント** | API の通信先 | 「窓口の番号」 |
| **REST API** | よく使われる API のルール | 「標準的な配達方法」 |
| **データベース** | データを保存する場所 | 「デジタル棚」 |
| **Entity** | データベースのテーブル構造 | 「表のヘッダー」 |
| **Repository** | データベースアクセス層 | 「データ棚の管理人」 |
| **Controller** | API の処理を仕切る | 「レストランの店長」 |

---

## 🚀 次回やるべきこと

### フロントエンド実装（次のステップ）

```
現状：
✅ バックエンド（サーバー）完成
❌ フロントエンド（画面）未実装

次やること：
1. React + TypeScript でフロント実装
2. Kanban ボード UI（画面）を作成
3. ドラッグ&ドロップ機能の実装
4. バックエンド API と連携
5. CREATE / UPDATE / DELETE のテスト
```

**実装内容（具体的に）：**

```
┌──────────────────────────────────────┐
│ ブラウザ画面（フロントエンド）       │
├──────────────────────────────────────┤
│ 📌 Kanban Task Manager               │
├────────────┬────────────┬────────────┤
│ TODO       │ IN_PROGRESS│ DONE       │
│ ┌────────┐ │ ┌────────┐ │ ┌────────┐ │
│ │タスク1 │ │ │タスク2 │ │ │タスク6 │ │
│ └────────┘ │ └────────┘ │ └────────┘ │
│            │            │            │
│ ┌────────┐ │ ┌────────┐ │ ┌────────┐ │
│ │タスク3 │ │ │タスク8 │ │ │タスク7 │ │
│ └────────┘ │ └────────┘ │ └────────┘ │
└────────────┴────────────┴────────────┘
      ↑ ドラッグ&ドロップで移動
```

---

## 📝 よくあるコマンド

### ローカル作業（あなたのパソコン）

```bash
# ---- ブランチ作業 ----

# ブランチ一覧確認
git branch

# ブランチ作成 & 切り替え
git checkout -b feature/xxx

# main ブランチに戻る
git checkout main

# ---- コード保存 ----

# コードの状態確認
git status

# コード追加（ステージング）
git add .

# コミット（保存）
git commit -m "メッセージ"

# ---- サーバーとの連携 ----

# サーバーに送信（プッシュ）
git push origin feature/xxx

# サーバーから受信（プル）
git pull origin main

# ---- マージ ----

# PR マージ（GitHub CLI）
gh pr merge 3 --merge --delete-branch

# または main に戻ってマージ
git checkout main
git pull origin main
git merge feature/xxx
git push origin main
```

### Docker で起動

```bash
# ディレクトリ移動
cd c:\AI\ Engineering\ Course\kanban-task-app

# コンテナ起動
docker-compose up --build

# コンテナ停止
docker-compose down

# ログ確認
docker logs kanban_backend
```

### API テスト（PowerShell）

```powershell
# 全タスク取得
curl http://localhost:8080/api/tasks

# 特定タスク取得
curl http://localhost:8080/api/tasks/1

# ステータスフィルタ
curl http://localhost:8080/api/tasks/status/IN_PROGRESS

# 新しいタスク作成
$body = @{
    title = "新タスク"
    description = "説明"
    status = "TODO"
    priority = 1
} | ConvertTo-Json

curl -Method POST `
  -Uri "http://localhost:8080/api/tasks" `
  -ContentType "application/json" `
  -Body $body
```

---

## 🎯 理解度チェック

### 次回開始時に確認してください：

```
Q1: Kanban Task App は何を作るアプリ？
A: タスク（TODO・IN_PROGRESS・DONE）を管理するアプリ

Q2: バックエンドとは？
A: サーバー側の処理（API、データベース）

Q3: API の主な 8 つのエンドポイントは？
A: GET全件、GET指定ID、GET・POSTステータス、
   GET優先度、POST作成、PUT更新、DELETE削除、GET健康診断

Q4: Git のワークフローの順序は？
A: Issue作成 → ブランチ作成 → コミット → PR → マージ

Q5: 次に作るものは？
A: フロントエンド（React UI、ドラッグ&ドロップ）
```

すべて「A」が言えたら、次のステップに進む準備 OK です！

---

## 📞 困ったときは

1. **このファイルを読む** ← 説明書
2. **TECH_SPEC.md** ← 技術仕様（詳細版）
3. **CLAUDE.md** ← ワークフロールール
4. **GitHub の Issues** ← 何やってるか記録

---

**最後に：**

今までやってきたことは、**アプリ開発の基礎**です。
- バックエンドは完成
- 次はフロントエンドで「見た目」を作る
- その次は「ログイン機能」などを追加

小さなステップを積み重ねるだけで、立派なアプリができます！
頑張ってください！🚀
